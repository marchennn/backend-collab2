# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class sensorActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.sensor]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await sensor.prisma().query_raw(
            'SELECT * FROM sensor WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.sensor
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await sensor.prisma().query_first(
            'SELECT * FROM sensor WHERE temperature = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.sensorCreateInput,
        include: Optional[types.sensorInclude] = None
    ) -> _PrismaModelT:
        """Create a new sensor record.

        Parameters
        ----------
        data
            sensor record data
        include
            Specifies which relations should be loaded on the returned sensor model

        Returns
        -------
        prisma.models.sensor
            The created sensor record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a sensor record from just the required fields
        sensor = await sensor.prisma().create(
            data={
                # data to create a sensor record
                'temperature': 'ggciceaie',
                'humidity': 'bbehjachib',
                'distance1': 'cadfabfehe',
                'distance2': 'dgiiaaijj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.sensorCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple sensor records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of sensor record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await sensor.prisma().create_many(
            data=[
                {
                    # data to create a sensor record
                    'temperature': 'bfaiacjjfc',
                    'humidity': 'eigcfgbif',
                    'distance1': 'bagcfbhiig',
                    'distance2': 'cghideieh',
                },
                {
                    # data to create a sensor record
                    'temperature': 'biabhbdai',
                    'humidity': 'idghgaicb',
                    'distance1': 'fjfddhigg',
                    'distance2': 'hjaecfifb',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.sensorWhereUniqueInput,
        include: Optional[types.sensorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single sensor record.

        Parameters
        ----------
        where
            sensor filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned sensor model

        Returns
        -------
        prisma.models.sensor
            The deleted sensor record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensor = await sensor.prisma().delete(
            where={
                'id': 2111915288,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.sensorWhereUniqueInput,
        include: Optional[types.sensorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique sensor record.

        Parameters
        ----------
        where
            sensor filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned sensor model

        Returns
        -------
        prisma.models.sensor
            The found sensor record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensor = await sensor.prisma().find_unique(
            where={
                'id': 1149758321,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.sensorWhereUniqueInput,
        include: Optional[types.sensorInclude] = None
    ) -> _PrismaModelT:
        """Find a unique sensor record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            sensor filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned sensor model

        Returns
        -------
        prisma.models.sensor
            The found sensor record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensor = await sensor.prisma().find_unique_or_raise(
            where={
                'id': 1644289366,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sensorWhereInput] = None,
        cursor: Optional[types.sensorWhereUniqueInput] = None,
        include: Optional[types.sensorInclude] = None,
        order: Optional[Union[types.sensorOrderByInput, List[types.sensorOrderByInput]]] = None,
        distinct: Optional[List[types.sensorScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple sensor records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of sensor records returned
        skip
            Ignore the first N results
        where
            sensor filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned sensor model
        order
            Order the returned sensor records by any field
        distinct
            Filter sensor records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.sensor]
            The list of all sensor records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 sensor records
        sensors = await sensor.prisma().find_many(take=10)

        # find the first 5 sensor records ordered by the humidity field
        sensors = await sensor.prisma().find_many(
            take=5,
            order={
                'humidity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.sensorWhereInput] = None,
        cursor: Optional[types.sensorWhereUniqueInput] = None,
        include: Optional[types.sensorInclude] = None,
        order: Optional[Union[types.sensorOrderByInput, List[types.sensorOrderByInput]]] = None,
        distinct: Optional[List[types.sensorScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single sensor record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            sensor filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned sensor model
        order
            Order the returned sensor records by any field
        distinct
            Filter sensor records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.sensor
            The first sensor record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second sensor record ordered by the distance1 field
        sensor = await sensor.prisma().find_first(
            skip=1,
            order={
                'distance1': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.sensorWhereInput] = None,
        cursor: Optional[types.sensorWhereUniqueInput] = None,
        include: Optional[types.sensorInclude] = None,
        order: Optional[Union[types.sensorOrderByInput, List[types.sensorOrderByInput]]] = None,
        distinct: Optional[List[types.sensorScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single sensor record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            sensor filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned sensor model
        order
            Order the returned sensor records by any field
        distinct
            Filter sensor records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.sensor
            The first sensor record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second sensor record ordered by the distance2 field
        sensor = await sensor.prisma().find_first_or_raise(
            skip=1,
            order={
                'distance2': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.sensorUpdateInput,
        where: types.sensorWhereUniqueInput,
        include: Optional[types.sensorInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single sensor record.

        Parameters
        ----------
        data
            sensor record data specifying what to update
        where
            sensor filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned sensor model

        Returns
        -------
        prisma.models.sensor
            The updated sensor record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        sensor = await sensor.prisma().update(
            where={
                'id': 1388290519,
            },
            data={
                # data to update the sensor record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.sensorWhereUniqueInput,
        data: types.sensorUpsertInput,
        include: Optional[types.sensorInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            sensor filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned sensor model

        Returns
        -------
        prisma.models.sensor
            The created or updated sensor record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        sensor = await sensor.prisma().upsert(
            where={
                'id': 1647418052,
            },
            data={
                'create': {
                    'id': 1647418052,
                    'temperature': 'biabhbdai',
                    'humidity': 'idghgaicb',
                    'distance1': 'fjfddhigg',
                    'distance2': 'hjaecfifb',
                },
                'update': {
                    'temperature': 'biabhbdai',
                    'humidity': 'idghgaicb',
                    'distance1': 'fjfddhigg',
                    'distance2': 'hjaecfifb',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.sensorUpdateManyMutationInput,
        where: types.sensorWhereInput,
    ) -> int:
        """Update multiple sensor records

        Parameters
        ----------
        data
            sensor data to update the selected sensor records to
        where
            Filter to select the sensor records to update

        Returns
        -------
        int
            The total number of sensor records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all sensor records
        total = await sensor.prisma().update_many(
            data={
                'timestamp': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sensorWhereInput] = None,
        cursor: Optional[types.sensorWhereUniqueInput] = None,
    ) -> int:
        """Count the number of sensor records present in the database

        Parameters
        ----------
        select
            Select the sensor fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            sensor filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.sensorCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await sensor.prisma().count()

        # results: prisma.types.sensorCountAggregateOutput
        results = await sensor.prisma().count(
            select={
                '_all': True,
                'update_at': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.sensorCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sensorWhereInput] = None,
        cursor: Optional[types.sensorWhereUniqueInput] = None,
    ) -> types.sensorCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.sensorCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.sensorWhereInput] = None,
        cursor: Optional[types.sensorWhereUniqueInput] = None,
    ) -> Union[int, types.sensorCountAggregateOutput]:
        """Count the number of sensor records present in the database

        Parameters
        ----------
        select
            Select the sensor fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            sensor filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.sensorCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await sensor.prisma().count()

        # results: prisma.types.sensorCountAggregateOutput
        results = await sensor.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.sensorCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.sensorWhereInput] = None
    ) -> int:
        """Delete multiple sensor records.

        Parameters
        ----------
        where
            Optional sensor filter to find the records to be deleted

        Returns
        -------
        int
            The total number of sensor records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all sensor records
        total = await sensor.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.sensorScalarFieldKeys'],
        *,
        where: Optional['types.sensorWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.sensorAvgAggregateInput'] = None,
        sum: Optional['types.sensorSumAggregateInput'] = None,
        min: Optional['types.sensorMinAggregateInput'] = None,
        max: Optional['types.sensorMaxAggregateInput'] = None,
        having: Optional['types.sensorScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.sensorCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.sensorScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.sensorScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.sensorGroupByOutput']:
        """Group sensor records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar sensor fields to group records by
        where
            sensor filter to select records
        take
            Limit the maximum number of sensor records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.sensorGroupByOutput]
            A list of dictionaries representing the sensor record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group sensor records by temperature values
        # and count how many records are in each group
        results = await sensor.prisma().group_by(
            ['temperature'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ledActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.led]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await led.prisma().query_raw(
            'SELECT * FROM led WHERE id = $1',
            1675546029,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.led
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await led.prisma().query_first(
            'SELECT * FROM led WHERE led1 = $1',
            'bhghchehcc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ledCreateInput,
        include: Optional[types.ledInclude] = None
    ) -> _PrismaModelT:
        """Create a new led record.

        Parameters
        ----------
        data
            led record data
        include
            Specifies which relations should be loaded on the returned led model

        Returns
        -------
        prisma.models.led
            The created led record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a led record from just the required fields
        led = await led.prisma().create(
            data={
                # data to create a led record
                'led1': 'dcgchcbbf',
                'led2': 'bdedcabahc',
                'led3': 'ghfhiafcb',
                'led4': 'heejgedji',
                'led5': 'bjgjgibgbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ledCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple led records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of led record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await led.prisma().create_many(
            data=[
                {
                    # data to create a led record
                    'led1': 'bbbgbhfjge',
                    'led2': 'igbehcbab',
                    'led3': 'bdadaadhag',
                    'led4': 'bgiggdidbf',
                    'led5': 'caaaedabfc',
                },
                {
                    # data to create a led record
                    'led1': 'bigibebcib',
                    'led2': 'bigaiehgcc',
                    'led3': 'beeifcbebf',
                    'led4': 'bgcigfahea',
                    'led5': 'bcejgaggif',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ledWhereUniqueInput,
        include: Optional[types.ledInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single led record.

        Parameters
        ----------
        where
            led filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned led model

        Returns
        -------
        prisma.models.led
            The deleted led record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        led = await led.prisma().delete(
            where={
                'id': 835903122,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ledWhereUniqueInput,
        include: Optional[types.ledInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique led record.

        Parameters
        ----------
        where
            led filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned led model

        Returns
        -------
        prisma.models.led
            The found led record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        led = await led.prisma().find_unique(
            where={
                'id': 763719779,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ledWhereUniqueInput,
        include: Optional[types.ledInclude] = None
    ) -> _PrismaModelT:
        """Find a unique led record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            led filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned led model

        Returns
        -------
        prisma.models.led
            The found led record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        led = await led.prisma().find_unique_or_raise(
            where={
                'id': 429995104,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ledWhereInput] = None,
        cursor: Optional[types.ledWhereUniqueInput] = None,
        include: Optional[types.ledInclude] = None,
        order: Optional[Union[types.ledOrderByInput, List[types.ledOrderByInput]]] = None,
        distinct: Optional[List[types.ledScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple led records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of led records returned
        skip
            Ignore the first N results
        where
            led filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned led model
        order
            Order the returned led records by any field
        distinct
            Filter led records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.led]
            The list of all led records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 led records
        leds = await led.prisma().find_many(take=10)

        # find the first 5 led records ordered by the led2 field
        leds = await led.prisma().find_many(
            take=5,
            order={
                'led2': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ledWhereInput] = None,
        cursor: Optional[types.ledWhereUniqueInput] = None,
        include: Optional[types.ledInclude] = None,
        order: Optional[Union[types.ledOrderByInput, List[types.ledOrderByInput]]] = None,
        distinct: Optional[List[types.ledScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single led record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            led filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned led model
        order
            Order the returned led records by any field
        distinct
            Filter led records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.led
            The first led record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second led record ordered by the led3 field
        led = await led.prisma().find_first(
            skip=1,
            order={
                'led3': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ledWhereInput] = None,
        cursor: Optional[types.ledWhereUniqueInput] = None,
        include: Optional[types.ledInclude] = None,
        order: Optional[Union[types.ledOrderByInput, List[types.ledOrderByInput]]] = None,
        distinct: Optional[List[types.ledScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single led record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            led filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned led model
        order
            Order the returned led records by any field
        distinct
            Filter led records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.led
            The first led record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second led record ordered by the led4 field
        led = await led.prisma().find_first_or_raise(
            skip=1,
            order={
                'led4': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ledUpdateInput,
        where: types.ledWhereUniqueInput,
        include: Optional[types.ledInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single led record.

        Parameters
        ----------
        data
            led record data specifying what to update
        where
            led filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned led model

        Returns
        -------
        prisma.models.led
            The updated led record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        led = await led.prisma().update(
            where={
                'id': 1775811865,
            },
            data={
                # data to update the led record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ledWhereUniqueInput,
        data: types.ledUpsertInput,
        include: Optional[types.ledInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            led filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned led model

        Returns
        -------
        prisma.models.led
            The created or updated led record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        led = await led.prisma().upsert(
            where={
                'id': 893145566,
            },
            data={
                'create': {
                    'id': 893145566,
                    'led1': 'bigibebcib',
                    'led2': 'bigaiehgcc',
                    'led3': 'beeifcbebf',
                    'led4': 'bgcigfahea',
                    'led5': 'bcejgaggif',
                },
                'update': {
                    'led1': 'bigibebcib',
                    'led2': 'bigaiehgcc',
                    'led3': 'beeifcbebf',
                    'led4': 'bgcigfahea',
                    'led5': 'bcejgaggif',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ledUpdateManyMutationInput,
        where: types.ledWhereInput,
    ) -> int:
        """Update multiple led records

        Parameters
        ----------
        data
            led data to update the selected led records to
        where
            Filter to select the led records to update

        Returns
        -------
        int
            The total number of led records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all led records
        total = await led.prisma().update_many(
            data={
                'led5': 'jjfeafhfj'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ledWhereInput] = None,
        cursor: Optional[types.ledWhereUniqueInput] = None,
    ) -> int:
        """Count the number of led records present in the database

        Parameters
        ----------
        select
            Select the led fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            led filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ledCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await led.prisma().count()

        # results: prisma.types.ledCountAggregateOutput
        results = await led.prisma().count(
            select={
                '_all': True,
                'timestamp': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ledCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ledWhereInput] = None,
        cursor: Optional[types.ledWhereUniqueInput] = None,
    ) -> types.ledCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ledCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ledWhereInput] = None,
        cursor: Optional[types.ledWhereUniqueInput] = None,
    ) -> Union[int, types.ledCountAggregateOutput]:
        """Count the number of led records present in the database

        Parameters
        ----------
        select
            Select the led fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            led filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ledCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await led.prisma().count()

        # results: prisma.types.ledCountAggregateOutput
        results = await led.prisma().count(
            select={
                '_all': True,
                'update_at': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ledCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ledWhereInput] = None
    ) -> int:
        """Delete multiple led records.

        Parameters
        ----------
        where
            Optional led filter to find the records to be deleted

        Returns
        -------
        int
            The total number of led records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all led records
        total = await led.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ledScalarFieldKeys'],
        *,
        where: Optional['types.ledWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ledAvgAggregateInput'] = None,
        sum: Optional['types.ledSumAggregateInput'] = None,
        min: Optional['types.ledMinAggregateInput'] = None,
        max: Optional['types.ledMaxAggregateInput'] = None,
        having: Optional['types.ledScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ledCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ledScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ledScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ledGroupByOutput']:
        """Group led records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar led fields to group records by
        where
            led filter to select records
        take
            Limit the maximum number of led records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ledGroupByOutput]
            A list of dictionaries representing the led record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group led records by id values
        # and count how many records are in each group
        results = await led.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class userActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.user]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await user.prisma().query_raw(
            'SELECT * FROM user WHERE id = $1',
            2102736524,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.user
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await user.prisma().query_first(
            'SELECT * FROM user WHERE device = $1',
            'chbfcacbd',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.userCreateInput,
        include: Optional[types.userInclude] = None
    ) -> _PrismaModelT:
        """Create a new user record.

        Parameters
        ----------
        data
            user record data
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The created user record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a user record from just the required fields
        user = await user.prisma().create(
            data={
                # data to create a user record
                'device': 'efggddide',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.userCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple user records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of user record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await user.prisma().create_many(
            data=[
                {
                    # data to create a user record
                    'device': 'caficfigfb',
                },
                {
                    # data to create a user record
                    'device': 'bfidgijfjc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single user record.

        Parameters
        ----------
        where
            user filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The deleted user record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().delete(
            where={
                'id': 878442065,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique user record.

        Parameters
        ----------
        where
            user filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The found user record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().find_unique(
            where={
                'id': 1675280054,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> _PrismaModelT:
        """Find a unique user record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            user filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The found user record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().find_unique_or_raise(
            where={
                'id': 1627576247,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
        include: Optional[types.userInclude] = None,
        order: Optional[Union[types.userOrderByInput, List[types.userOrderByInput]]] = None,
        distinct: Optional[List[types.userScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple user records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of user records returned
        skip
            Ignore the first N results
        where
            user filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned user model
        order
            Order the returned user records by any field
        distinct
            Filter user records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.user]
            The list of all user records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 user records
        users = await user.prisma().find_many(take=10)

        # find the first 5 user records ordered by the create_at field
        users = await user.prisma().find_many(
            take=5,
            order={
                'create_at': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
        include: Optional[types.userInclude] = None,
        order: Optional[Union[types.userOrderByInput, List[types.userOrderByInput]]] = None,
        distinct: Optional[List[types.userScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single user record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            user filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned user model
        order
            Order the returned user records by any field
        distinct
            Filter user records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.user
            The first user record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second user record ordered by the update_at field
        user = await user.prisma().find_first(
            skip=1,
            order={
                'update_at': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
        include: Optional[types.userInclude] = None,
        order: Optional[Union[types.userOrderByInput, List[types.userOrderByInput]]] = None,
        distinct: Optional[List[types.userScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single user record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            user filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned user model
        order
            Order the returned user records by any field
        distinct
            Filter user records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.user
            The first user record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second user record ordered by the id field
        user = await user.prisma().find_first_or_raise(
            skip=1,
            order={
                'id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.userUpdateInput,
        where: types.userWhereUniqueInput,
        include: Optional[types.userInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single user record.

        Parameters
        ----------
        data
            user record data specifying what to update
        where
            user filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The updated user record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await user.prisma().update(
            where={
                'id': 2054802212,
            },
            data={
                # data to update the user record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.userWhereUniqueInput,
        data: types.userUpsertInput,
        include: Optional[types.userInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            user filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned user model

        Returns
        -------
        prisma.models.user
            The created or updated user record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        user = await user.prisma().upsert(
            where={
                'id': 60335757,
            },
            data={
                'create': {
                    'id': 60335757,
                    'device': 'bfidgijfjc',
                },
                'update': {
                    'device': 'bfidgijfjc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.userUpdateManyMutationInput,
        where: types.userWhereInput,
    ) -> int:
        """Update multiple user records

        Parameters
        ----------
        data
            user data to update the selected user records to
        where
            Filter to select the user records to update

        Returns
        -------
        int
            The total number of user records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all user records
        total = await user.prisma().update_many(
            data={
                'device': 'gieegcbeg'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
    ) -> int:
        """Count the number of user records present in the database

        Parameters
        ----------
        select
            Select the user fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            user filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await user.prisma().count()

        # results: prisma.types.userCountAggregateOutput
        results = await user.prisma().count(
            select={
                '_all': True,
                'create_at': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.userCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
    ) -> types.userCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.userCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.userWhereInput] = None,
        cursor: Optional[types.userWhereUniqueInput] = None,
    ) -> Union[int, types.userCountAggregateOutput]:
        """Count the number of user records present in the database

        Parameters
        ----------
        select
            Select the user fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            user filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.userCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await user.prisma().count()

        # results: prisma.types.userCountAggregateOutput
        results = await user.prisma().count(
            select={
                '_all': True,
                'update_at': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.userCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.userWhereInput] = None
    ) -> int:
        """Delete multiple user records.

        Parameters
        ----------
        where
            Optional user filter to find the records to be deleted

        Returns
        -------
        int
            The total number of user records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all user records
        total = await user.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.userScalarFieldKeys'],
        *,
        where: Optional['types.userWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.userAvgAggregateInput'] = None,
        sum: Optional['types.userSumAggregateInput'] = None,
        min: Optional['types.userMinAggregateInput'] = None,
        max: Optional['types.userMaxAggregateInput'] = None,
        having: Optional['types.userScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.userCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.userScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.userScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.userGroupByOutput']:
        """Group user records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar user fields to group records by
        where
            user filter to select records
        take
            Limit the maximum number of user records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.userGroupByOutput]
            A list of dictionaries representing the user record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group user records by id values
        # and count how many records are in each group
        results = await user.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models